# Critical Rules (MUST READ FIRST)

## NEVER Do

- **TEMPORARY ADS CREDENTIAL RPC EXCEPTION (owner-approved 2026-08-26 in PR #3381; expires when Ads credentials move to a restricted worker role or on 2026-09-16, whichever comes first):** only authenticated, trusted-merchant-resolved, merchant-scoped call graphs rooted at the Google, Meta, TikTok, and Snapchat Ads `accounts`, `callback`, `disconnect`, and `sync` routes may use `apps/web/src/lib/ads/server-credential-client.ts`, and only after `integrations:manage` authorization; callback routes must additionally pass OAuth state/nonce validation. The branded `createServiceClient('ads-credentials')` client is limited to the exact credential RPCs for reading, storing, rotating, selecting, reauthorization-marking, and deleting Ads grants; nonce, reporting/status, and analytics reads remain on the authenticated RLS client, and spend replacement remains on the separate spend boundary. The helper and all credential call graphs are server-only and must never enter a client graph. No raw or body-selected tenant identity, sibling route, generic service-role operation, table query, additional RPC, importer, or re-export inherits authorization. Ciphertexts, plaintext provider tokens, provider response bodies, and service-role material must never enter responses, logs, event payloads, or client bundles. The backend key inherently bypasses RLS; these controls constrain approved use, not that capability. Remove the exception when the restricted worker role is available, or obtain explicit reapproval by the fixed expiry.
- **NEVER** use the admin/service-role Supabase client for user-facing operations. Use `@/lib/supabase/server` for SSR and `@/lib/supabase/client` for browser.
- **TEMPORARY LEGACY ANALYTICS EXCEPTION (owner-approved 2026-07-18; expires when queue-only delivery activates or on 2026-09-16, whichever comes first):** only these exact server-side edges are authorized: (1) `apps/web/src/app/api/analytics/conversion/route.ts -> apps/web/src/lib/analytics/trusted-server-ad-platform-fanout.ts`, where the route may construct the branded `createServiceClient('event-pipeline')`; (2) `apps/web/src/app/api/events/route.ts -> apps/web/src/lib/analytics/trusted-server-ad-platform-fanout.ts`, under the same branded-client rule; and (3) the existing byte-frozen `apps/web/src/app/api/platform/events/route.ts -> apps/web/src/app/api/platform/events/platform-event-forwarding.ts -> apps/web/src/lib/supabase/admin.ts#createAdminClient()` edge. These edges are server-only by contract and must never be imported, re-exported, or bundled into a client graph. The two merchant edges may construct the privileged client only after tenant identity is independently resolved from trusted host/domain context and matches any request value. Their authority is limited to the exact merchant entitlement, merchant credential, and feature-setting projections needed for legacy analytics/conversion fanout; the platform edge retains only its exact platform-settings projection. Raw or body-selected tenant identity confers no authority; mismatch or unverified paths construct no privileged client. Credential values must never enter responses, logs, event payloads, or client bundles. No sibling module, fourth importer/edge, or generic service-role route use inherits authorization. The owner acknowledges that the backend key inherently bypasses RLS; these controls constrain approved use, not that capability. Remove or re-approve this exception at expiry; reapproval is required even if queue-only activation has not occurred.
- **TEMPORARY ADS SPEND SYNC WRITE EXCEPTION (owner-approved 2026-08-25 in PR #3381; expires when Ads spend writes move to a restricted worker role or on 2026-09-16, whichever comes first):** only the four authenticated, CSRF-protected call graphs rooted at `apps/web/src/app/api/integrations/ads/google/sync/route.ts`, `apps/web/src/app/api/integrations/ads/meta/sync/route.ts`, `apps/web/src/app/api/integrations/ads/tiktok/sync/route.ts`, and `apps/web/src/app/api/integrations/ads/snapchat/sync/route.ts` may construct the branded client through `apps/web/src/lib/ads/server-spend-client.ts`, and only after validated input, resolved merchant access, and `integrations:manage` permission. The privileged client is limited to the final `replace_google_ads_spend_daily` or `replace_merchant_ads_spend_daily_window` RPC; credential reads, token updates, account selection, and sync markers remain on the authenticated RLS client. The helper, routes, and sync modules are server-only and must never enter a client graph. No request-selected tenant identity, sibling route, generic service-role operation, table query, additional RPC, importer, or re-export inherits authorization. Credentials, provider bodies, and service-role material must never enter responses, logs, or event payloads. The backend key inherently bypasses RLS; these controls constrain the approved call graphs, not that capability. Remove the exception when the restricted worker role is available, or obtain explicit reapproval by the fixed expiry.
- **TEMPORARY B0 CACHE-INVALIDATION EXCEPTION (owner-approved 2026-07-27; expires when B0 is retired in favor of queue-owned invalidation or on 2026-09-16, whichever comes first):** only `apps/web/src/app/api/cron/drain-cache-invalidations/route.ts` may construct the branded `createServiceClient('event-pipeline')`, and only after `CRON_SECRET` authentication succeeds. That route may pass validated claims to `apps/web/src/lib/drain-storefront-cache-invalidation.ts` and may use the branded client only for `claim_cache_invalidations`, `finish_cache_invalidation` (including failed or completed outcomes), and the aggregate-boolean-only `has_cache_invalidation_dead_letters` alert RPC. The route, drainer, and strict Cloudflare helper are server-only and must never be imported, re-exported, or bundled into a client graph. Service-role credentials, Cloudflare credentials, tenant secrets, and provider response bodies must never enter responses, logs, event payloads, or client bundles. No sibling route, helper, importer, re-export, generic service-role operation, table query, or additional RPC inherits authorization. The backend key inherently bypasses RLS; these controls constrain the approved edge, not that capability. Remove the exception with B0 retirement, or obtain explicit reapproval by the fixed expiry even if queue-owned invalidation is not yet active.
- **TEMPORARY GIGL TRACKING WORKER EXCEPTION (owner-approved 2026-08-01 in PR #3248; expires when GIGL tracking moves to a restricted worker role or on 2026-09-16, whichever comes first):** only the two CRON_SECRET-authenticated call graphs rooted at `apps/web/src/app/api/cron/gigl-tracking/route.ts` and `apps/web/src/app/api/cron/gigl-tracking-notifications/route.ts` may construct the branded `createServiceClient('event-pipeline')`, and only after authentication succeeds. The monitor graph is limited to the GIGL claim, failure, apply, release, and pause RPCs; the notification graph is limited to the GIGL notification claim, begin-dispatch, and complete-dispatch RPCs plus the exact event, order, customer, push-token, push-attempt, receipt, and insurance-reminder projections required for those notifications. The workers may call the existing push and insurance helpers only for that tracking-notification fanout; provider bodies, credentials, arbitrary table access, tenant-selected identity, and service-role clients must never enter responses, logs, event payloads, or client bundles. No user-facing route, sibling route, unrelated helper, importer, re-export, generic service-role operation, table query, or additional RPC inherits this exception. The backend key inherently bypasses RLS; these controls constrain the approved call graphs, not that capability. Remove the exception when the restricted worker role is available, or obtain explicit reapproval by the fixed expiry.
- **TEMPORARY WALLET FUNDING RECOVERY HMAC EXCEPTION (owner-approved 2026-09-06 in PR #3435; expires when HMAC provisioning moves to a restricted worker role or on 2026-09-16, whichever comes first):** only the CRON_SECRET-authenticated call graph rooted at `apps/web/src/app/api/cron/provision-wallet-funding-recovery-hmac/route.ts` may construct the branded client through `apps/web/src/lib/wallet/server-funding-recovery-hmac-client.ts`, and only after authentication succeeds. The privileged client is limited to the exact `set_merchant_wallet_funding_recovery_hmac_secret` RPC. The helper, route, and provisioner are server-only and must never enter a client graph. No sibling route, generic service-role operation, table query, additional RPC, importer, or re-export inherits authorization. The recovery HMAC secret, service-role material, and provider bodies must never enter responses, logs, event payloads, or client bundles. The backend key inherently bypasses RLS; these controls constrain the approved call graph, not that capability. Remove the exception when the restricted worker role is available, or obtain explicit reapproval by the fixed expiry.
- **TEMPORARY SHIPPING QUOTE BOOKING ECONOMICS EXCEPTION (owner-approved 2026-09-06 in PR #3435; expires when booking economics move to a restricted worker role or on 2026-09-16, whichever comes first):** only authenticated, merchant-scoped booking/fulfillment call graphs that reach `apps/web/src/lib/shipping/shipping-quote-booking-economics.ts` may construct the branded client through `apps/web/src/lib/shipping/server-shipping-quote-booking-economics-client.ts`, and only after merchant auth succeeds. The privileged client is limited to the exact `get_shipping_quote_booking_economics` RPC. The helper and call graphs are server-only and must never enter a client graph. No raw or body-selected tenant identity, sibling route, generic service-role operation, table query, additional RPC, importer, or re-export inherits authorization. Internal tariff/margin values, service-role material, and provider bodies must never enter responses, logs, event payloads, or client bundles. The backend key inherently bypasses RLS; these controls constrain the approved call graphs, not that capability. Remove the exception when the restricted worker role is available, or obtain explicit reapproval by the fixed expiry.
- **NEVER** modify `proxy.ts` without explicit approval — it handles auth, CSRF, rate limiting, and custom domains. A bug here breaks everything.
- **NEVER** use `dangerouslySetInnerHTML`. Use the sanitization utilities in `lib/sanitize*.ts`.
- **NEVER** edit existing migration files in `supabase/migrations/`. Migrations are append-only.
- **NEVER** add manual `React.memo`, `useCallback`, or `useMemo` — React Compiler handles memoization.
- **NEVER** add ESLint config files or plugins — we use Biome exclusively.
- **NEVER** use npm or yarn — this is a pnpm monorepo. Use `pnpm turbo <command>`.
- **NEVER** consume Vercel build minutes from Codex by running cloud-building deploy commands such as `vercel`, `vercel --prod`, or `vercel deploy --prod` without `--prebuilt`. Production deploys must use a local/prebuilt CI build and finish with `vercel deploy --prebuilt --prod`; the default runner is the VPS prebuilt flow on `bassey@82.29.190.219`, but owner-approved emergency fallback may use a GitHub-hosted runner for the same prebuilt flow. `vercel inspect` and `vercel logs` are allowed for status/debugging.
- **NEVER** use `select('*')` — always select specific columns.


## ALWAYS Do

- **ALWAYS** validate API inputs with Zod before any database operation.
- **ALWAYS** check auth (`supabase.auth.getUser()`) as the first operation in protected API routes.
- **ALWAYS** add RLS policies when creating new database tables.
- **ALWAYS** run `pnpm turbo lint && pnpm turbo typecheck` after writing or modifying code.
- **ALWAYS** include a regression test when fixing a bug — the test must reproduce the exact failing condition.
- **ALWAYS** follow the Boy Scout Rule: when editing a file over 300 lines, extract the logic you touched into smaller files (hooks, utils, sub-components, schemas). Don't refactor the whole file — just leave it better than you found it.

## High-Risk Areas

- The `payments/webhook` routes process real money. Triple-check any changes.
- The `src/ai/` directory has per-user rate limits. Respect them in any modifications.
- `proxy.ts` is the middleware — auth, CSRF, rate limiting, custom domains all flow through it.
