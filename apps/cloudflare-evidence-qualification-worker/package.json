{
  "name": "@baci/cloudflare-evidence-qualification-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "test": "vitest run --config ../../packages/shared/vitest.config.ts",
    "typecheck": "mkdir -p .qualification-dist/types && wrangler types .qualification-dist/types/version-a.d.ts --config wrangler.version-a.jsonc && wrangler types .qualification-dist/types/version-b.d.ts --config wrangler.version-b.jsonc && tsc -p tsconfig.json && tsc -p tsconfig.version-b.json && tsc -p tsconfig.artifact-receipt.json"
  },
  "devDependencies": {
    "comment-json": "4.6.2",
    "vitest": "^4.1.11",
    "wrangler": "4.115.0"
  }
}
