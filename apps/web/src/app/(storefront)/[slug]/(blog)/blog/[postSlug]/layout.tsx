import '@/app/(storefront)/storefront-blog.css';
import '@/app/(storefront)/storefront-blog-post.css';
import '@/app/(storefront)/storefront-core.css';
import type { ReactNode } from 'react';

export default function StorefrontBlogPostCssLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
