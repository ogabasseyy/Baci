import '@/app/(storefront)/storefront-blog.css';
import '@/app/(storefront)/storefront-core.css';
import type { ReactNode } from 'react';

export function StorefrontEagerBlogCssLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
