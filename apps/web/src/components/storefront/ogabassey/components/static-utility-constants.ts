/**
 * Shared static-utility configuration for the hero utility panel twins.
 * Dedicated module per the repository's constants rule: the interactive
 * panel and its zero-JS static twin must agree on the default active
 * option, so the index lives here instead of inside either component.
 */

/** Default visible promo word (index into the rotating words). */
export const STATIC_ACTIVE_WORD_INDEX = 0;
