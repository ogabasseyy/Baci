/**
 * Web alias for the shared Peppol type-code resolver (single source of
 * truth also used by the mobile receipt preview). Kept so existing
 * imports and tests keep working.
 */
export { resolveInvoiceTypeCode } from '@baci/shared';
